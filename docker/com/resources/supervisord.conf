[supervisord]
nodaemon=true
logfile=/var/tmp/supervisord.log
pidfile=/var/tmp/supervisord.pid

[program:tango-starter]
command=/bin/bash -c '
  sudo chmod 755 /usr/local/bin/tango_register_device &&
  /usr/local/bin/tango_register_device Starter/`hostname --short` Starter tango/admin/`hostname --short` &&
  /usr/local/bin/tango_register_device SerialLine/SerialLine_`hostname --short` SerialLine sys/serial/serial1 &&
  /usr/local/bin/tango_register_device ModbusDS/ModbusDS_`hostname --short` ModbusDS sys/modbusds/modbus1 &&
  exec -- /usr/local/bin/Starter `hostname --short`'
autorestart=true
startsecs=0
startretries=5
priority=1

[program:tango-serial]
command=/bin/bash -c '
  sudo chmod 755 /usr/local/bin/SerialLine
  exec -- /usr/local/bin/SerialLine SerialLine_`hostname --short` -D'
autorestart=true
startsecs=0
priority=2

[program:tango-modbus]
command=/bin/bash -c '
  sudo chmod 755 /usr/local/bin/ModbusDS
  exec -- /usr/local/bin/ModbusDS ModbusDS_`hostname --short` -D'
autorestart=true
startsecs=0
priority=3

#add PyPLC